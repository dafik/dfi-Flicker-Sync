- each item, i in sequence
    - if (i != sequence.length - 1)
        li(data-type="#{item.type}" data-id="#{item.id}")
            a(href="#") #{item.name}
            span.divider /
    - else
        li.active(data-type="#{item.type}" data-id="#{item.id}")
            a(href="#") #{item.name}